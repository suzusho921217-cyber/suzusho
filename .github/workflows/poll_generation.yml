# §5 poll_generation: 生成完了確認→DL→品質判定。
name: poll_generation
on:
  schedule:
    - cron: "*/20 * * * *"
  workflow_dispatch:

concurrency:
  group: sns-pipeline-state
  cancel-in-progress: false

jobs:
  call:
    uses: ./.github/workflows/_reusable.yml
    with:
      command: poll-generation
    secrets: inherit
